{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "project.pug",
          "style.css",
          "osm-routing.png",
          "documentation.pdf",
          "lib/**",
          "data/**"
        ]
      }
    }
  ],
  "routes": [
    { "src": "/project.js", "dest": "/project.js" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}